import {ResourceWithOptions} from "admin-bro";
import {ContactFormEntity} from "../../entities/contact-form.entity";

const ContactFormResource: ResourceWithOptions = {
    resource: ContactFormEntity,
    options: {},
};

export default ContactFormResource;
