import {ResourceWithOptions} from "admin-bro";
import {Blog} from "../../entities/blog.entity";

const BlogResource: ResourceWithOptions = {
    resource: Blog,
    options: {},
};

export default BlogResource;
